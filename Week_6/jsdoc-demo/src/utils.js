/**
 * @file this file contains helper functions
 */

/**
 * @module utilities
 */

/**
 * Function to uppercase a given text
 * @param {string} str a text that has to be uppercased
 * @returns {string}
 */

function uppercaseText(str) {
  return str.toUpperCase();
}

module.exports = {
  uppercaseText,
};
